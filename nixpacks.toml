[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "dotnet --info",
  "dotnet restore Travel.sln",
  "dotnet publish Travel.Api/Travel.Api.csproj -c Release -o out"
]

[start]
cmd = "dotnet out/Travel.Api.dll"
